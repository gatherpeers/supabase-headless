x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "20m"
    max-file: "3"

services:

  db:
    build:
      context: ./db
      dockerfile: Dockerfile
    logging: *default-logging
    container_name: supabase-db
    restart: always
    security_opt:
      - "no-new-privileges:true"
    deploy:
      resources:
        limits:
          cpus: ${DB_CPU_LIMIT:-4.0}
          memory: ${DB_MEMORY_LIMIT:-4G}
    shm_size: ${DB_SHM_SIZE:-2GB}
    command: >
      postgres
      -c port=${POSTGRES_PORT:-5432}
      -c shared_buffers=${POSTGRES_SHARED_BUFFERS:-1GB}
      -c effective_cache_size=${POSTGRES_EFFECTIVE_CACHE_SIZE:-3GB}
      -c maintenance_work_mem=${POSTGRES_MAINTENANCE_WORK_MEM:-512MB}
      -c max_connections=${POSTGRES_MAX_CONNECTIONS:-100}
      -c wal_level=logical
      -c max_replication_slots=10
      -c max_slot_wal_keep_size=4GB
      -c max_wal_senders=10
      -c log_min_messages=warning
      -c work_mem=${POSTGRES_WORK_MEM:-32MB}
      -c log_min_duration_statement=${POSTGRES_LOG_MIN_DURATION_STATEMENT:-200}
      -c shared_preload_libraries='pg_stat_statements'
      -c pg_stat_statements.track=all
      -c password_encryption=scram-sha-256
    environment:
      # Database
      POSTGRES_DB: ${POSTGRES_DB:-postgres}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST_AUTH_METHOD: scram-sha-256
      POSTGRES_INITDB_ARGS: "--locale=C.UTF-8 --encoding=UTF8 --text-search-config=simple"
      PGDATA: /var/lib/postgresql/data

      # Bootstrap roles (db/init.sql)
      AUTH_DB_PASSWORD: ${AUTH_DB_PASSWORD}
      STORAGE_DB_PASSWORD: ${STORAGE_DB_PASSWORD}
      PGRST_AUTH_USER: ${PGRST_AUTH_USER:-authenticator}
      PGRST_AUTH_PASSWORD: ${PGRST_AUTH_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - private_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -p ${POSTGRES_PORT:-5432} -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 60s

  # Automatic database migrations: stack -> app (if mounted by user). Runs psql, thus never starts a Postgres server.
  db-migrate:
    image: postgres:18.4-alpine
    container_name: supabase-db-migrate
    restart: "no"
    security_opt:
      - "no-new-privileges:true"
    depends_on:
      db:
        condition: service_healthy
      rest:
        condition: service_healthy
      auth:
        condition: service_healthy
      storage:
        condition: service_healthy
      realtime:
        condition: service_healthy
    environment:
      # Database (libpq)
      PGHOST: db
      PGPORT: ${POSTGRES_PORT:-5432}
      PGUSER: ${POSTGRES_USER:-postgres}
      PGPASSWORD: ${POSTGRES_PASSWORD}
      PGDATABASE: ${POSTGRES_DB:-postgres}
    volumes:
      - ./db/migrate.sh:/migrate.sh:ro
      - ./db/stack/schema.sql:/stack/schema.sql:ro
      - ./db/stack/migrations:/stack/migrations:ro
      - ./db/app/schema.sql:/app/schema.sql:ro
      - ./db/app/migrations:/app/migrations:ro
    command: ["sh", "/migrate.sh"]
    networks:
      - private_net

  # Isolated from the default compose, used to generate on-demand database types.
  postgres-meta:
    image: supabase/postgres-meta:v0.96.6
    container_name: supabase-postgres-meta
    profiles: ['meta']
    restart: always
    security_opt:
      - "no-new-privileges:true"
    logging: *default-logging
    depends_on:
      db:
        condition: service_healthy
      db-migrate:
        condition: service_completed_successfully
    environment:
      # Server
      PG_META_HOST: 0.0.0.0
      PG_META_PORT: 8080

      # Database
      PG_META_DB_HOST: db
      PG_META_DB_PORT: ${POSTGRES_PORT:-5432}
      PG_META_DB_NAME: ${POSTGRES_DB:-postgres}
      PG_META_DB_USER: ${POSTGRES_USER:-postgres}
      PG_META_DB_PASSWORD: ${POSTGRES_PASSWORD}
    networks:
      - private_net

  auth:
    image: supabase/auth:v2.193.0
    logging: *default-logging
    container_name: supabase-auth
    restart: always
    security_opt:
      - "no-new-privileges:true"
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Server
      GOTRUE_API_HOST: 0.0.0.0
      GOTRUE_API_PORT: 9999
      GOTRUE_DB_DRIVER: postgres
      GOTRUE_DB_DATABASE_URL: postgres://supabase_auth_admin:${AUTH_DB_PASSWORD}@db:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-postgres}?sslmode=disable
      # Explicit pool cap. Upstream default is 0 (unlimited); bound it so it fits the Postgres connection budget (see db/README.md).
      GOTRUE_DB_MAX_POOL_SIZE: ${AUTH_DB_POOL_SIZE:-10}

      # Public URLs / redirects
      # /auth/v1 prefix included -> GoTrue emits OAuth callbacks, SAML endpoints and email links under the gateway's auth path (custom OAuth providers work out of the box, SAML SSO lives at /auth/v1/sso/saml/*).
      API_EXTERNAL_URL: ${PUBLIC_API_URL}${AUTH_PREFIX}
      GOTRUE_SITE_URL: ${APP_URL}
      GOTRUE_URI_ALLOW_LIST: ${GOTRUE_URI_ALLOW_LIST}

      # Auth policy
      GOTRUE_EXTERNAL_EMAIL_ENABLED: ${GOTRUE_EXTERNAL_EMAIL_ENABLED:-true}
      GOTRUE_MAILER_AUTOCONFIRM: ${GOTRUE_MAILER_AUTOCONFIRM:-true}
      GOTRUE_SECURITY_REFRESH_TOKEN_ROTATION_ENABLED: ${GOTRUE_SECURITY_REFRESH_TOKEN_ROTATION_ENABLED:-true}

      # JWT (from generate-keys / .env)
      GOTRUE_JWT_SECRET: ${JWT_SECRET}
      GOTRUE_JWT_KEYS: ${JWT_KEYS}
      GOTRUE_JWT_EXP: ${GOTRUE_JWT_EXP}
      GOTRUE_JWT_AUD: authenticated
      GOTRUE_JWT_ISSUER: ${PUBLIC_API_URL}${AUTH_PREFIX}
      # Still necessary despite deprecation warnings. Without it, GoTrue issues user JWTs with an empty "role" claim.
      GOTRUE_JWT_DEFAULT_GROUP_NAME: authenticated

      # Observability
      GOTRUE_RATE_LIMIT_HEADER: ${GOTRUE_RATE_LIMIT_HEADER:-X-Forwarded-For}
      REQUEST_ID_HEADER: ${REQUEST_ID_HEADER:-X-Request-ID}
      GOTRUE_LOG_LEVEL: ${GOTRUE_LOG_LEVEL:-warn}
    networks:
      - private_net
      - public_net
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--spider", "http://127.0.0.1:9999/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  realtime:
    image: supabase/realtime:v2.113.2
    logging: *default-logging
    container_name: supabase-realtime
    restart: always
    security_opt:
      - "no-new-privileges:true"
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Server
      PORT: 4000
      APP_NAME: realtime
      ERL_AFLAGS: -proto_dist inet_tcp
      DNS_NODES: "''"
      RLIMIT_NOFILE: "10000"
      REALTIME_IP_VERSION: ipv4
      RUN_JANITOR: "true"

      # Database
      DB_HOST: db
      DB_PORT: ${POSTGRES_PORT:-5432}
      DB_NAME: ${POSTGRES_DB:-postgres}
      DB_USER: ${POSTGRES_USER:-postgres}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      # Authorization (RLS check) pool. Realtime also opens ~1 connection for broadcast-from-database replication; budget accordingly (see db/README.md).
      DB_POOL_SIZE: ${REALTIME_DB_POOL_SIZE:-5}
      DB_AFTER_CONNECT_QUERY: "SET search_path TO _realtime, extensions, public"
      DB_ENC_KEY: ${REALTIME_DB_ENC_KEY}
      # Silence IPv4 warnings
      DB_IP_VERSION: ipv4
      DB_SSL: "false"
      # DB_SSL_CA_CERT:

      # Auth / crypto
      API_JWT_SECRET: ${JWT_SECRET}
      API_JWT_JWKS: ${JWT_JWKS}
      SECRET_KEY_BASE: ${REALTIME_SECRET_KEY_BASE}
      METRICS_JWT_SECRET: ${JWT_SECRET}

      # Replication / self-host tenant
      # Max 63 chars total: supabase_realtime_messages_replication_slot_ (45) + suffix (≤18).
      SLOT_NAME_SUFFIX: ${REALTIME_SLOT_NAME_SUFFIX:-local}
      SEED_SELF_HOST: "true"
      SELF_HOST_TENANT_NAME: ${PUBLIC_API_DOMAIN}

      # Dashboard
      DASHBOARD_AUTH: ${REALTIME_DASHBOARD_AUTH:-basic_auth}
      DASHBOARD_USER: ${REALTIME_DASHBOARD_USER:-admin}
      DASHBOARD_PASSWORD: ${REALTIME_DASHBOARD_PASSWORD}

      # Observability
      DISABLE_HEALTHCHECK_LOGGING: "true"
      LOG_LEVEL: ${REALTIME_LOG_LEVEL:-warning}
    networks:
      - private_net
    healthcheck:
      # Unauthenticated liveness probe (PageController :healthcheck route, no auth pipeline; returns 200 "ok"). Avoids the tenant /health endpoint, whose :api pipeline only accepts an HS256 token signed by JWT_SECRET (i.e. the legacy ANON_KEY) — there is no JWKS/ES256 path — so legacy keys stay optional. DB readiness is gated separately via depends_on db: service_healthy.
      test: ["CMD-SHELL", "curl -sSf -o /dev/null http://127.0.0.1:4000/healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  rest:
    image: postgrest/postgrest:v14.15
    logging: *default-logging
    container_name: supabase-rest
    restart: always
    security_opt:
      - "no-new-privileges:true"
    depends_on:
      db:
        condition: service_healthy
    environment:
      # Server
      PGRST_SERVER_HOST: 0.0.0.0
      PGRST_ADMIN_SERVER_HOST: 127.0.0.1
      PGRST_ADMIN_SERVER_PORT: 3001

      # Database
      PGRST_DB_URI: postgres://${PGRST_AUTH_USER:-authenticator}:${PGRST_AUTH_PASSWORD}@db:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-postgres}
      PGRST_DB_SCHEMAS: ${PGRST_DB_SCHEMAS:-public}
      PGRST_DB_EXTRA_SEARCH_PATH: ${PGRST_DB_EXTRA_SEARCH_PATH:-public,extensions}
      PGRST_DB_ANON_ROLE: ${PGRST_DB_ANON_ROLE:-anon}
      PGRST_DB_MAX_ROWS: ${PGRST_DB_MAX_ROWS:-1000}
      PGRST_DB_POOL: ${PGRST_DB_POOL:-20}

      # JWT — JWKS so PostgREST can verify both ES256 (asymmetric) and HS256 (symmetric) tokens.
      PGRST_JWT_SECRET: ${JWT_JWKS}

      # Public URL
      PGRST_OPENAPI_SERVER_PROXY_URI: ${PUBLIC_API_URL}${REST_PREFIX:-/rest/v1}

      # Observability
      PGRST_SERVER_TRACE_HEADER: ${REQUEST_ID_HEADER:-X-Request-ID}
      PGRST_LOG_LEVEL: ${PGRST_LOG_LEVEL:-warn}
    networks:
      - private_net
    healthcheck:
      test: ["CMD", "postgrest", "--ready"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  rustfs:
    image: rustfs/rustfs:1.0.0-beta.8
    container_name: supabase-rustfs
    restart: always
    logging: *default-logging
    security_opt:
      - "no-new-privileges:true"
    environment:
      # Credentials
      RUSTFS_ACCESS_KEY: ${RUSTFS_ACCESS_KEY}
      RUSTFS_SECRET_KEY: ${RUSTFS_SECRET_KEY}

      # Server / region
      RUSTFS_REGION: ${S3_REGION:-local}
      RUSTFS_CONSOLE_ENABLE: ${RUSTFS_CONSOLE_ENABLE:-false}
      RUSTFS_CONSOLE_ADDRESS: 0.0.0.0:9001
      RUSTFS_CONSOLE_CORS_ALLOWED_ORIGINS: "*"
      RUSTFS_CORS_ALLOWED_ORIGINS: "*"

      # Observability
      RUST_LOG: ${RUSTFS_LOG_LEVEL:-warn}
    volumes:
      - rustfs_data:/data
    networks:
      - private_net
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://127.0.0.1:9000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  rustfs-createbucket:
    image: rustfs/rc:v0.1.26
    container_name: supabase-rustfs-createbucket
    restart: "no"
    security_opt:
      - "no-new-privileges:true"
    depends_on:
      rustfs:
        condition: service_healthy
    entrypoint: >
      /bin/sh -ec "
      rc alias set supa-rustfs http://rustfs:9000 ${RUSTFS_ACCESS_KEY} ${RUSTFS_SECRET_KEY} &&
      rc mb --ignore-existing supa-rustfs/${GLOBAL_S3_BUCKET:-supabase-global}
      "
    networks:
      - private_net

  imgproxy:
    image: darthsim/imgproxy:v4.0.11
    container_name: supabase-imgproxy
    restart: always
    security_opt:
      - "no-new-privileges:true"
    depends_on:
      rustfs:
        condition: service_healthy
      rustfs-createbucket:
        condition: service_completed_successfully
    deploy:
      resources:
        limits:
          cpus: '1.5'
          memory: 1G
    logging: *default-logging
    environment:
      # Server
      IMGPROXY_BIND: :5001
      # Reachable only from private_net (not exposed publicly). `IMGPROXY_KEY` and `IMGPROXY_SALT` aren't defined thus produce a warning, omit safely

      # S3 backend (RustFS)
      IMGPROXY_USE_S3: "true"
      IMGPROXY_S3_ENDPOINT: http://rustfs:9000
      AWS_REGION: ${S3_REGION:-local}
      AWS_ACCESS_KEY_ID: ${RUSTFS_ACCESS_KEY}
      AWS_SECRET_ACCESS_KEY: ${RUSTFS_SECRET_KEY}
      AWS_S3_FORCE_PATH_STYLE: "true"

      # Transforms / limits
      IMGPROXY_USE_ETAG: "true"
      IMGPROXY_AUTO_WEBP: "true"
      IMGPROXY_MAX_SRC_FILE_SIZE: ${FILE_SIZE_LIMIT:-52428800}
      IMGPROXY_FAIL_ON_DEPRECATION: ${IMGPROXY_FAIL_ON_DEPRECATION:-true}

      # Observability
      IMGPROXY_LOG_LEVEL: ${IMGPROXY_LOG_LEVEL:-warn}
    networks:
      - private_net
    healthcheck:
      test: ["CMD", "imgproxy", "health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  storage:
    image: supabase/storage-api:v1.66.3
    logging: *default-logging
    container_name: supabase-storage
    restart: always
    security_opt:
      - "no-new-privileges:true"
    depends_on:
      db:
        condition: service_healthy
      auth:
        condition: service_healthy
      rest:
        condition: service_healthy
      imgproxy:
        condition: service_healthy
      rustfs:
        condition: service_healthy
      rustfs-createbucket:
        condition: service_completed_successfully
    environment:
      # Auth / JWT
      ANON_KEY: ${ANON_KEY_ASYMMETRIC}
      SERVICE_KEY: ${SERVICE_ROLE_KEY_ASYMMETRIC}
      PGRST_JWT_SECRET: ${JWT_JWKS}
      JWT_JWKS: ${JWT_JWKS}
      JWT_CACHING_ENABLED: ${STORAGE_JWT_CACHING_ENABLED:-true}
      TENANT_ID: ${STORAGE_TENANT_ID:-default}

      # Database
      DATABASE_URL: postgres://supabase_storage_admin:${STORAGE_DB_PASSWORD}@db:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-postgres}
      # Explicit pool cap (upstream default is 20). Keep it within the Postgres connection budget (see db/README.md).
      DATABASE_MAX_CONNECTIONS: ${STORAGE_DB_POOL_SIZE:-15}

      # S3 backend (RustFS)
      STORAGE_BACKEND: s3
      GLOBAL_S3_BUCKET: ${GLOBAL_S3_BUCKET:-supabase-global}
      GLOBAL_S3_ENDPOINT: http://rustfs:9000
      GLOBAL_S3_FORCE_PATH_STYLE: "true"
      REGION: ${S3_REGION:-local}
      AWS_ACCESS_KEY_ID: ${RUSTFS_ACCESS_KEY}
      AWS_SECRET_ACCESS_KEY: ${RUSTFS_SECRET_KEY}

      # Public URL / gateway prefix
      STORAGE_PUBLIC_URL: ${PUBLIC_API_URL}
      # Honour the gateway's X-Forwarded-Prefix header so S3 SigV4 signature verification and TUS upload Location URLs reconstruct the original /storage/v1 prefix correctly.
      REQUEST_ALLOW_X_FORWARDED_PATH: "true"

      # Uploads
      FILE_SIZE_LIMIT: ${FILE_SIZE_LIMIT:-52428800}
      TUS_URL_PATH: ${STORAGE_PREFIX:-/storage/v1}/upload/resumable
      TUS_ALLOW_S3_TAGS: "false"

      # Image transforms
      ENABLE_IMAGE_TRANSFORMATION: "true"
      IMGPROXY_URL: http://imgproxy:5001

      # Observability
      REQUEST_ID_HEADER: ${REQUEST_ID_HEADER:-X-Request-ID}
      LOG_LEVEL: ${STORAGE_LOG_LEVEL:-warn}
    networks:
      - private_net
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--spider", "http://127.0.0.1:5000/status"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  functions:
    build:
      context: ./functions
      dockerfile: Dockerfile
    container_name: supabase-edge-functions
    restart: always
    security_opt:
      - "no-new-privileges:true"
    logging: *default-logging
    depends_on:
      db:
        condition: service_healthy
      auth:
        condition: service_healthy
      storage:
        condition: service_healthy
    environment:
      # Supabase client (injected into user functions)
      SUPABASE_URL: http://gateway
      SUPABASE_PUBLIC_URL: ${PUBLIC_API_URL}
      SUPABASE_ANON_KEY: ${ANON_KEY_ASYMMETRIC}
      SUPABASE_SERVICE_ROLE_KEY: ${SERVICE_ROLE_KEY_ASYMMETRIC}
    volumes:
      - ./functions/app:/home/deno/functions:ro
    networks:
      - private_net
      - public_net
    healthcheck:
      test: ["CMD-SHELL", "timeout 2 bash -c 'exec 3<>/dev/tcp/127.0.0.1/9000 && printf \"GET /_internal/health HTTP/1.0\\r\\nHost: localhost\\r\\n\\r\\n\" >&3 && head -1 <&3 | grep -q \"200 OK\"'"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  gateway:
    build:
      context: ./caddy
      dockerfile: Dockerfile
    logging: *default-logging
    container_name: supabase-gateway
    restart: always
    security_opt:
      - "no-new-privileges:true"
    # Expose :80/:443 only after upstreams and app SQL migrations are ready.
    depends_on:
      db-migrate:
        condition: service_completed_successfully
      auth:
        condition: service_healthy
      rest:
        condition: service_healthy
      realtime:
        condition: service_healthy
      storage:
        condition: service_healthy
      functions:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - ./logs/caddy:/var/log/caddy
    environment:
      # Server / TLS
      PUBLIC_API_DOMAIN: ${PUBLIC_API_DOMAIN}
      CADDY_CONTACT_EMAIL: ${CADDY_CONTACT_EMAIL:-admin@localhost}
      CADDY_LOG_LEVEL: ${CADDY_LOG_LEVEL:-WARN}

      # CORS / rate limit
      CORS_ALLOWED_ORIGIN: ${CORS_ALLOWED_ORIGIN}
      GATEWAY_RATE_LIMIT_EVENTS: ${GATEWAY_RATE_LIMIT_EVENTS:-100}
      GATEWAY_RATE_LIMIT_WINDOW: ${GATEWAY_RATE_LIMIT_WINDOW:-1s}
      REQUEST_ID_HEADER: ${REQUEST_ID_HEADER:-X-Request-ID}

      # Path prefixes
      AUTH_PREFIX: ${AUTH_PREFIX:-/auth/v1}
      REST_PREFIX: ${REST_PREFIX:-/rest/v1}
      REALTIME_PREFIX: ${REALTIME_PREFIX:-/realtime/v1}
      STORAGE_PREFIX: ${STORAGE_PREFIX:-/storage/v1}
      FUNCTIONS_PREFIX: ${FUNCTIONS_PREFIX:-/functions/v1}
      REALTIME_DASHBOARD_PREFIX: ${REALTIME_DASHBOARD_PREFIX:-/admin}

      # API key translation: opaque sb_* keys -> internal asymmetric JWTs, plus accept-list of asymmetric (*_ASYMMETRIC) and legacy (ANON_KEY / SERVICE_ROLE_KEY HS256) JWTs that pass through the gateway untouched. The `:-__unset_*__` defaults ensure each map input stays unique even when an env var is unset or empty (Caddy's `map` directive rejects duplicate inputs at config-load time, and an empty string for two rows would collide).
      SUPABASE_PUBLISHABLE_KEY: ${SUPABASE_PUBLISHABLE_KEY:-__unset_publishable_key__}
      SUPABASE_SECRET_KEY: ${SUPABASE_SECRET_KEY:-__unset_secret_key__}
      ANON_KEY_ASYMMETRIC: ${ANON_KEY_ASYMMETRIC:-__unset_anon_key_asymmetric__}
      SERVICE_ROLE_KEY_ASYMMETRIC: ${SERVICE_ROLE_KEY_ASYMMETRIC:-__unset_service_role_key_asymmetric__}
      ANON_KEY: ${ANON_KEY:-__unset_legacy_anon_key__}
      SERVICE_ROLE_KEY: ${SERVICE_ROLE_KEY:-__unset_legacy_service_role_key__}
    networks:
      - public_net
      - private_net
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:2019/config/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  db_data:
  rustfs_data:
  caddy_data:

networks:
  public_net:
    driver: bridge
  private_net:
    driver: bridge
    internal: true